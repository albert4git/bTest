#include <stdio.h>
/* This is a comment. */
int main(int argc, char *argv[])
{
        // this is also a comment
        int distance = 100;
        // this is also a comment
        printf("You are %d miles away.\n", distance);
        return 0;
}
